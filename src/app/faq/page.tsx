import ComingSoon from '@/components/ComingSoon';

export const metadata = { title: 'FAQs — Bondok Fried Chicken' };

export default function Page() {
  return <ComingSoon title={`FAQs`} text={`Frequently asked questions will be published here.`} />;
}
