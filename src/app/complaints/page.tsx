import ComingSoon from '@/components/ComingSoon';

export const metadata = { title: 'Complaints & Suggestions — Bondok Fried Chicken' };

export default function Page() {
  return <ComingSoon title={`Complaints & Suggestions`} text={`We want to hear from you. The feedback form is being prepared.`} />;
}
