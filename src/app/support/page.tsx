import ComingSoon from '@/components/ComingSoon';

export const metadata = { title: 'Support — Bondok Fried Chicken' };

export default function Page() {
  return <ComingSoon title={`Support`} text={`Need help with an order? Our support center is being prepared. For now, call your nearest branch.`} />;
}
